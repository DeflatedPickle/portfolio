mixin Project(projectLogo, projectName, projectLink, projectDescription, projectImages)
    div.project
        if projectLogo
            img(src=projectLogo)
        h3= projectName
        if projectLink
            a(href=projectLink).projectLink Website
        p= projectDescription

        // if Object.keys(projectImages).length > 0
        if projectImages
            div.expander
                h2.expanderText

            div(class="hiddenContent hidden")
                ol
                    each data, name in projectImages
                        li
                            img(src=data[0] alt=data[1]).projectImage
                            p= name